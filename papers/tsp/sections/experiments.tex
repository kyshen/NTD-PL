\section{Experiments}
\label{sec:experiments}

The experiments evaluate the proposed model through three connected questions.
First, does the learned response behave as predicted on controlled nonlinear
measurements?
Second, under the same Tucker rank, does the learned response improve
real tensor reconstruction and completion consistently across scenes, with
separate stress tests for missing entries and observation noise? Third, can a
low-cost diagnostic indicate when the response model is appropriate? These questions
match the modeling claim: NTD-PL is expected to be most beneficial when a
matched-rank Tucker model leaves residual structure consistent with a shared
entrywise response. Unless stated otherwise, percentage gains denote relative
error reduction from Tucker to NTD-PL. The main real-data evidence is based on
all CAVE scenes under matched Tucker rank; noisy reconstruction and
cross-domain diagnostics are used to test robustness and operating conditions.

\subsection{Controlled Nonlinear Tensor Recovery}

The controlled experiment isolates the measurement-response mechanism without
missing entries or observation noise. We start from an underlying Tucker signal
$\cS^\star$ of known rank and generate the measured tensor by adding a
nonlinear response component that is orthogonal to the constant and linear
channels. Let $\bar{\cS}$ denote the normalized underlying signal and let
$\bar{\cR}$ denote the normalized nonlinear residual after this projection. The
clean measurement is generated as
\begin{equation}
  \cY_\alpha =
  \mathrm{scale}_{[0,1]}
  \left(
  \sqrt{1-\alpha}\,\bar{\cS}
  +
  \sqrt{\alpha}\,\bar{\cR}
  \right),
\end{equation}
where $\alpha$ controls nonlinear residual energy after the constant and linear
response components have been removed. This construction keeps the intended Tucker rank fixed while making
the measured tensor increasingly mismatched to a linear Tucker observation
model.

Figure~\ref{fig:controlled-alpha} shows the expected transition. When
$\alpha$ is small, the measured tensor is close to the underlying Tucker signal and
NTD-PL stays close to matched-rank Tucker. As the nonlinear residual energy
increases, the learned response explains a growing part of the residual and the
gap widens across all four target responses: $s^2$, $s^2+s^3$,
$\tanh(\kappa s)$, and $(e^{\kappa s}-1)/\kappa$. The
degree-dependent behavior is reported in
Appendix~\ref{app:controlled_details}.

All experiments in the main text use the power dictionary because it is
compact, interpretable, and directly supports the rank-inflation analysis in
Section~\ref{sec:theory}. Appendix~\ref{app:controlled_details} adds
controlled details, including a linear-response case in which high-order
channels remain inactive. These full-observation experiments serve as mechanism
checks; masked recovery and real tensor recovery are evaluated in the following
subsections.

\begin{figure*}[!t]
  \centering
  \includegraphics[width=0.92\textwidth]{figures/controlled_nonlinear_alpha_grid.pdf}
  \caption{Controlled nonlinear residual sweep for four target responses. The
  parameter $\alpha$ controls nonlinear residual energy after removing constant
  and linear components. NTD-PL remains close to Tucker in the nearly linear
  regime and separates as the nonlinear component grows.}
  \label{fig:controlled-alpha}
\end{figure*}

\subsection{Hyperspectral Tensor Recovery}

We next evaluate whether the same linked Tucker representation improves
real spectral-spatial tensors. Unless otherwise stated, hyperspectral cubes are
globally normalized, the Tucker rank is fixed across Tucker and
NTD-PL, and completion metrics are computed only on held-out entries. We report
root mean square error (RMSE) and spectral angle mapper (SAM); lower values are
better for both.

\subsubsection{CAVE Reconstruction and Completion}

We first use the CAVE multispectral image database \cite{cave_multispectral_database,
yasuma2010gapc}, which provides full spectral-spatial scenes and therefore
supports both full-observation reconstruction and masked completion. Full
reconstruction tests whether the learned response adds a compact correction at a
fixed Tucker rank. Across the tested ranks, NTD-PL consistently improves
matched-rank Tucker in both RMSE and SAM when averaged over all 15 scenes, with
larger gains at lower ranks and smaller gains as the Tucker rank grows; the
full capacity curves are reported in Appendix~\ref{app:hsi_protocol}. This is
the expected behavior of a fixed-rank nonlinear correction: if ordinary multilinear
rank is already high enough to absorb the nonlinear response, the marginal
value of the response should decrease.

Table~\ref{tab:rank-lift-tucker} asks a complementary capacity question: how
much ordinary Tucker rank is needed to match a lower-rank NTD-PL model? Across
the tested ranks, Tucker needs roughly 51--60\% more parameters to match the
NTD-PL RMSE, while NTD-PL still has lower SAM. The result supports the
rank-inflation interpretation in Section~\ref{sec:theory}: the power response
reuses the same low-rank Tucker signal to express structured nonlinear
residuals with a small number of response coefficients.

We then add observation noise only to the full-reconstruction setting. The
model is fitted to clean or noisy full CAVE cubes and evaluated against the
corresponding clean cube. This separates radiometric robustness from missing
entry recovery. Across clean observations and Gaussian noise levels down to
10 dB, NTD-PL maintains positive RMSE and SAM gains over matched-rank Tucker;
the full table is reported in Appendix~\ref{app:hsi_protocol}.

\begin{table*}[!t]
  \centering
  \caption{Higher-rank Tucker comparison on CAVE reconstruction. For each
  NTD-PL Tucker rank, we report the first higher-rank Tucker model matching
  NTD-PL RMSE and the additional parameter cost.}
  \label{tab:rank-lift-tucker}
  \scriptsize
  \input{tables/rank_lift_tucker_full_sweep.tex}
\end{table*}

Held-out completion tests whether the fixed-rank benefit transfers to
unobserved entries without introducing observation noise. Table~\ref{tab:cave-completion-main}
reports CAVE completion under random masks using all 15 scenes and three mask
seeds per scene. At the same Tucker rank, NTD-PL improves missing-entry RMSE
by 12.5--17.7\% and missing-entry SAM by 13.4--29.3\% across the tested missing
rates. The gains are stable across mask density, and paired scene-level
summaries are reported in Appendix~\ref{app:hsi_protocol}.

\begin{table*}[!t]
  \centering
  \caption{CAVE random-missing completion at matched Tucker rank. Metrics are
  evaluated only on held-out entries; gains are relative improvements over
  matched-rank Tucker.}
  \label{tab:cave-completion-main}
  \scriptsize
  \input{tables/cave_completion_main.tex}
\end{table*}

\subsection{Rank-Inflation Evidence}
\label{sec:exp-rank-inflation}

The reconstruction and completion tables show that the linked model improves
standard accuracy metrics. We next ask whether this improvement is accompanied
by the rank-inflation behavior predicted by the analysis. For this mechanism
view, we use the full CAVE scenes at their native $512\times512$ spatial
resolution and fix the Tucker rank to $(12,12,4)$ for both Tucker and NTD-PL.
We sweep the maximum power degree $P$ and compute the spatial-mode spectra of
the learned Tucker signal, the final prediction, and the response component
$f_{\bbeta}(\cS)-\cS$.

Figure~\ref{fig:mechanism-rank-inflation} shows two complementary summaries.
Increasing $P$ raises both the reconstruction gain and the effective rank of
the response component, while the learned Tucker signal itself remains at the
prescribed rank. Moreover, the fraction of the measured tensor's spatial tail
energy captured by the NTD-PL prediction is strongly aligned with the RMSE
gain across scenes and degrees. The Spearman correlation between spatial tail
capture and RMSE gain is $0.94$ in this sweep. Table~\ref{tab:rank-inflation-mechanism}
reports the corresponding degree-wise summary.

\begin{figure*}[!t]
  \centering
  \includegraphics[width=0.92\textwidth]{figures/mechanism_rank_inflation.pdf}
  \caption{Rank-inflation evidence on full-size CAVE reconstruction at Tucker
  rank $(12,12,4)$. Left: increasing the polynomial degree raises the
  response-component rank and reconstruction gain. Right: scene-level tail
  capture by the NTD-PL prediction tracks RMSE gain.}
  \label{fig:mechanism-rank-inflation}
\end{figure*}

\input{tables/mechanism_rank_inflation.tex}

\subsection{Learned Response Curves}
\label{sec:exp-learned-response}

The previous experiment measures rank inflation in the tensor domain. We now
look directly at the learned scalar response. For each full-size CAVE scene, we
fit NTD-PL at rank $(12,12,4)$ and evaluate the learned map
$s\mapsto f_{\bbeta}(s)$ on percentiles of the learned Tucker signal. Because
Tucker factors have scale and sign gauges, each curve is plotted after endpoint
normalization on the central percentile range; the curve shape, rather than the
absolute latent scale, is the object of interest.

Figure~\ref{fig:mechanism-learned-response} shows that the learned response is
not an arbitrary high-degree oscillation. The mean curve is consistently
nonlinear and mostly monotone, and the shape changes little after moderate
degree. The numerical summary in Table~\ref{tab:learned-response-mechanism}
matches this visual pattern: moving from $P=1$ to $P=4$ increases both the
nonlinear deviation from the best affine fit and the RMSE gain, while the
additional change from $P=4$ to $P=6$ is smaller. This provides a direct
response-level view of the same mechanism: the model does not replace the
low-rank signal by a separate high-rank tensor, but learns a shared scalar
calibration of that signal.

\begin{figure*}[!t]
  \centering
  \includegraphics[width=0.92\textwidth]{figures/mechanism_learned_response.pdf}
  \caption{Learned response curves on full-size CAVE reconstruction at Tucker
  rank $(12,12,4)$. Left: endpoint-normalized response curves for $P=6$; thin
  lines are scenes and the thick line is their mean. Right: nonlinear deviation
  and gain over the linear response increase with degree and then stabilize.}
  \label{fig:mechanism-learned-response}
\end{figure*}

\input{tables/mechanism_learned_response.tex}

\subsection{Spectral-Regime Error Analysis}
\label{sec:exp-error-regime}

Finally, we ask where the reconstruction error is reduced in the observed
tensor. Entries are grouped by their normalized observed value and by spectral
band. This is an observed-domain analysis: the intensity bins are bins of
entries of the normalized measured tensor, not bins of RGB brightness or
spatial pixel averages. The analysis connects the learned response curve to
observable error behavior by showing which response ranges receive the largest
correction.

Figure~\ref{fig:mechanism-error-regime} shows that the gain is not spread
uniformly over the cube. The largest relative improvements occur in low-to-mid
intensity deciles, whereas one high-intensity decile is nearly neutral. At the
same time, the highest-intensity decile contributes a large share of the total
MSE reduction because its baseline error energy is large. The spectral view
shows stronger gains in the green-to-red regimes than at the shortest
wavelengths. Table~\ref{tab:error-regime-mechanism} gives the regime-level
summary: the 510--600 nm and 610--700 nm regimes account for most of the MSE
reduction in this full-size CAVE setting. These patterns support the response
interpretation from another angle: NTD-PL changes the effective calibration of
specific intensity and spectral regimes rather than applying a uniform
improvement everywhere.

\begin{figure*}[!t]
  \centering
  \includegraphics[width=0.96\textwidth]{figures/mechanism_error_regime.pdf}
  \caption{Observed-domain error regimes on full-size CAVE reconstruction at
  Tucker rank $(12,12,4)$ and $P=6$. Left: RMSE gain by normalized-intensity
  decile. Middle: gain by spectral band. Right: joint intensity--spectral
  regimes. Positive values indicate lower error for NTD-PL than Tucker.}
  \label{fig:mechanism-error-regime}
\end{figure*}

\input{tables/mechanism_error_regime.tex}

\subsection{Diagnostics and Operating Regimes}

The diagnostic $\Dlink$ is computed by fitting a Tucker model and measuring
the residual reduction obtained by refitting only the response coefficients.
It is therefore substantially cheaper than full NTD-PL training and is used
here as a model-selection signal for the proposed recovery model.
Table~\ref{tab:cave-link-yield-summary} groups CAVE completion scenes
by diagnostic tercile. Higher $\Dlink$ corresponds to larger held-out RMSE gains
at every missing rate, while the low-diagnostic group has little or negative
average gain. The Spearman correlations, 0.829--0.889, show that the diagnostic
is strongly aligned with the eventual benefit of the full model.

\begin{table}[!t]
  \centering
  \caption{CAVE completion diagnostic. Scenes are grouped by $\Dlink$ tercile;
  entries are mean NTD-PL RMSE gains over matched-rank Tucker.}
  \label{tab:cave-link-yield-summary}
  \scriptsize
  \input{tables/ntdpl_polycal_diagnostic.tex}
\end{table}

Figure~\ref{fig:nonhsi-link-scatter} evaluates the same diagnostic beyond
hyperspectral data. Each point is an independently fitted tensor unit, and the
horizontal axis is $\Dlink$ measured before full NTD-PL training. Object-view
tensors show the clearest separation, action-video tensors follow the same
trend, and natural images occupy a lower-yield but still positive regime. These
results reinforce the main interpretation: NTD-PL is most effective when the
Tucker residual contains structure that is shared across entries through the
same response function.

\begin{figure*}[!t]
  \centering
  \includegraphics[width=0.86\textwidth]{figures/nonhsi_dlink_scatter_3domain.pdf}
  \caption{Cross-domain response diagnostic. Each point plots the diagnostic
  $\Dlink$ against the RMSE gain of full NTD-PL over Tucker. Larger
  diagnostic yield predicts larger benefit from the learned response.}
  \label{fig:nonhsi-link-scatter}
\end{figure*}
