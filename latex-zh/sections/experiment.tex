\section{实验}

本章在统一协议下报告 NTD-PL 在合成与真实高光谱数据上的实验结果。实验依次包括共享设置、可控合成数据、以及真实高光谱上的恢复与补全，用于说明方法的适用范围、收益来源和计算代价。

\subsection{实验设置}
\label{sec:exp-setup}

本节给出后续实验共享的数据生成原则、比较口径与评价指标。除各小节特有的实现细节外，所有结果均遵循同一套可复现实验协议。

\paragraph{合成数据生成原则.}
为避免不同非线性映射下“非线性强度”的定义不一致，本文将 $\alpha$ 定义为\textbf{正交非线性残差的能量占比}，而不是直接乘在非线性函数前的幅值系数。设线性低秩骨架为
\begin{equation}
\mathcal S^\star \in \mathbb R^{I_1\times \cdots \times I_N},
\end{equation}
给定逐元素映射 $g(\cdot)$ 后，先构造相对 $\mathcal S^\star$ 的正交残差
\begin{equation}
\mathcal R^\star
=
g(\mathcal S^\star)
-
\frac{\langle g(\mathcal S^\star), \mathcal S^\star\rangle}{\|\mathcal S^\star\|_F^2}\,\mathcal S^\star,
\end{equation}
再归一化为
\begin{equation}
\widetilde{\mathcal R}^\star
=
\frac{\|\mathcal S^\star\|_F}{\|\mathcal R^\star\|_F}\,\mathcal R^\star.
\end{equation}
最终观测张量定义为
\begin{equation}
\mathcal Y_\alpha
=
\sqrt{1-\alpha}\,\mathcal S^\star
+
\sqrt{\alpha}\,\widetilde{\mathcal R}^\star,
\qquad 0\le \alpha\le 1.
\label{eq:exp-alpha-energy}
\end{equation}
因此，$\alpha$ 表示的是观测中分配给正交非线性残差的能量比例。随着 $\alpha$ 增大，观测会逐步偏离线性低秩骨架，而不是仅仅放大观测幅值。对于线性一致性小节中出现的 \texttt{仿射偏移控制}，其本质是在线性 Tucker 源数据上加入常数偏置后再归一化。

\paragraph{非线性类型、比较方法与主指标.}
可控合成实验考虑两类多项式残差族和两类平滑残差族：
\begin{equation}
g_{\mathrm{poly2}}(s)=s^2,\qquad
g_{\mathrm{poly3}}(s)=s^2+s^3,
\end{equation}
\begin{equation}
g_{\sin}(s)=\sin(\kappa_{\sin}s),\qquad
g_{\tanh}(s)=\tanh(\kappa_{\tanh}s).
\end{equation}
其中 $\kappa_{\sin}$ 与 $\kappa_{\tanh}$ 根据输入尺度自适应设定，以避免函数长期停留在线性区间或过早饱和。

实验以 NMSE(dB) 和RMSE 作为主指标。真实高光谱实验中，全观测重构统一报告 RMSE、NMSE(dB) 与 SAM；随机缺失补全优先报告 RMSE*、NMSE(dB)*、SAM*。其中带 $*$ 的指标表示仅在 missing entries 上计算，是补全质量的主判断依据。

对于后文涉及的 $\beta_p$ 项贡献百分比，本文统一采用
\begin{equation}
c_p
=
\frac{\|\beta_p \mathcal S^{\odot p}\|_F}{\sum_{q=0}^{P}\|\beta_q \mathcal S^{\odot q}\|_F}\times 100\%.
\label{eq:beta-contribution}
\end{equation}
该定义刻画的是各阶项在整体展开中的相对强度分配， $\{c_p\}$ 对 $p$ 求和后恰好等于 $100\%$。

\paragraph{基线方法.}
本文比较的线性低秩基线包括 Tucker、CP、TT 和 TR。Tucker 分解是本文最直接的线性参照。CP 分解将张量表示为若干秩一张量之和，具有更紧凑的参数化形式，也是低秩张量建模中最经典的基线之一 \cite{kolda2009tensor}. TT 分解采用链式核分解高阶张量，可显著降低高阶场景中的存储与计算复杂度 \cite{oseledets2011}. TR 分解进一步将链式结构闭合为环形，在秩分配上比 TT 更灵活 \cite{zhao2016tr}. 这些方法都不显式引入条目层非线性，因此可用于检验 NTD-PL 的收益是否确实来自共享低秩骨架上的多项式链接，而不是单纯的参数规模变化。

在上述共享协议下，下面先报告可控合成数据实验。该部分分别考察严格线性源数据下的线性一致性，以及统一非线性残差生成协议下的匹配与逼近表现。

\subsection{可控合成数据实验}
\label{sec:exp-syn}

本节在可控机制下比较 Tucker 系线性基线与 NTD-PL。实验包含两部分：一是在线性源数据下比较 Tucker 与线性受限 NTD-PL 的配对误差，二是在统一非线性残差协议下比较不同方法随 $\alpha$ 和 $P$ 的误差变化。

\subsubsection{线性一致性验证}
\label{sec:linear-consistency}

在线性源数据下，我们比较 Tucker 与NTD-PL 的配对误差，以检验后者是否精确退化到 Tucker。具体地，数据为多个随机种子下生成的 Tucker 张量，秩为 $[4,4,4]$，加上仿射偏置\texttt{bias}，然后进行能量归一化与 $30$ dB 噪声叠加；NTD-PL 取$P=6$，可选$\beta_0$是否引入。

表~\ref{tab:linear-consistency} 显示，在\texttt{bias=0}时，Tucker 与不引入常数项的 NTD-PL 的拟合误差几乎完全重合，高次项贡献占比很低，证明 NTD-PL 线性一致；在\texttt{bias=0.5}时，NTD-PL依然能较好地拟合，而Tucker和$\beta_0$受限的NTD-PL则出现系统性偏差。有趣的是，可以看到 NTD-PL 在$\beta_0$受限时自动通过高次项来拟合偏置，结果仍然略优于Tucker。

\begin{table}[htbp]
    \centering
    \caption{比较纯线性源数据上的Tucker和NTD-PL，用于检验 NTD-PL 的线性一致性}
    \label{tab:linear-consistency}
    \renewcommand{\arraystretch}{1.15}
    \small
    \resizebox{\linewidth}{!}{%
    \input{inputs/linear-consistency/table.tex}}
\end{table}

\subsubsection{非线性映射匹配与逼近实验}

在统一的非线性生成协议下 (见 \ref{sec:exp-setup})，我们考虑多项式残差和广义非线性残差两种情形。具体地，数据由多个随机种子下生成的 Tucker 张量出发，分别施加 \texttt{poly2}、\texttt{poly3}、\texttt{sin} 与 \texttt{tanh} 四类逐元素非线性残差，并按式~\eqref{eq:exp-alpha-energy} 控制非线性能量占比 $\alpha$。实验统一扫描 $\alpha\in\{0.10,0.15,0.20,0.25,0.30,0.35,0.40\}$ 与 $P\in\{1,2,3,4,5,6\}$，并在 $10$ 个随机种子上比较相近参数量下， Tucker、CP、TT、TR 与 NTD-PL 的 NMSE(dB) 表现。

图~\ref{fig:nonlinear-alpha-grid} 表明，在各类非线性残差下，NTD-PL 在整个非线性强度区间内保持稳定领先，说明 NTD-PL 在处理非线性残差时具有优势。另外，可以看到NTD-PL的逼近误差随$\alpha$缩放，结果趋势与推论\ref{cor:alpha_truncation_app}（见附录）相符合。表~\ref{tab:beta-contrib-nonlinear} 给出 NTD-PL 各 $\beta_p$ 项下的平均贡献及其标准差，可以看出，在数据对应的主导阶次上，相应项贡献突出。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{./inputs/figures/main/nonlinear_alpha_grid.pdf}
    \caption{NTD-PL及基线方法的逼近误差随非线性强度的变化}
    \label{fig:nonlinear-alpha-grid}
\end{figure}

\begin{table}[htbp]
    \centering
    \caption{NTD-PL各次数项的贡献（单位：\%）}
    \label{tab:beta-contrib-nonlinear}
    \renewcommand{\arraystretch}{1.15}
    \small
    \resizebox{\linewidth}{!}{%
    \input{inputs/nonlinear-approx/beta_contribution_table.tex}}
\end{table}

图~\ref{fig:nonlinear-pmax-grid} 清晰地展示了不同次数$P$下的NTD-PL的拟合效果。可以看到，$P=1$时的性能与Tucker相近，又一次说明了NTD-PL的线性一致性，随着多项式次数$P$的增加，NTD-PL 的性能逐渐优于Tucker基线。该结果表明，NTD-PL 确实能够逐步吸收非线性残差。进一步地，性能提升主要集中在较低到中等阶数范围内，而在更高阶时增益趋于饱和，说明对这类平滑链路而言，有限阶多项式链接已经能够提供较充分的近似。对于 \texttt{poly2} 和 \texttt{poly3} ，NTD-PL 均在对应次数（$P=2$和$P=3$）时出现系统性匹配。一个有意思的现象是，性能提升并非随次数增加而均匀出现。对于 \texttt{sin} 和 \texttt{tanh}，明显的改进主要发生在奇数阶提升处，而相邻偶数阶带来的额外收益较弱。考虑到这两类链路都具有奇对称性，这一现象说明，在当前近似零中心的实验协议下，新增的有效表达能力更可能集中在奇次项上。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{./inputs/figures/main/nonlinear_pmax_grid.pdf}
    \caption{NTD-PL的逼近误差随次数 $P$ 的变化}
    \label{fig:nonlinear-pmax-grid}
\end{figure}

图~\ref{fig:rmse_step_grid}展示了 NTD-PL 逐次增加的策略下的训练损失曲线，可以看到次数增加的时刻往往伴随阶跃式的增益。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{./inputs/figures/main/nonlinear_step_grid.pdf}
    \caption{NTD-PL在逐次增加策略下的训练损失曲线}
    \label{fig:rmse_step_grid}
\end{figure}

\subsection{高光谱数据上的非线性低秩重构与补全}
\label{sec:real_hsi_representation}

本节转向真实高光谱数据上的实验验证，重点回答三个问题：第一，NTD-PL 相对线性 Tucker 的整体收益是否在真实场景中依然成立；第二，这种收益究竟来自哪里；第三，这一优势是否具有明确的空间定位、阶数机制和跨数据集稳定性。围绕这三个问题，后文依次从全观测重构、随机缺失补全、机制区分、局部收益定位、阶数饱和分析以及跨数据集鲁棒性六个方面展开，从而形成一条由整体性能、机制解释到外部验证的完整证据链。

\subsubsection{数据与协议}

实验分为主机制分析与外部鲁棒性验证两层。主实验采用 CAVE \cite{cave_multispectral_database,yasuma2010gapc} 数据集中的 $15$ 个场景，用于系统分析 NTD-PL 在真实高光谱张量上的整体收益、局部收益分布以及阶数机制；外部验证则进一步采用 Jasper Ridge \cite{roberts1993jasperridge}、Samson \cite{davis2007samson}、Urban \cite{urban_hypercube_dataset} 和 Cuprite \cite{resmini1997cuprite} 四个常用基线场景，用于检验主结论在不同空间尺寸、波段数和场景属性下是否仍然成立。

所有真实数据实验均采用全观测，保持原数据尺寸，不额外裁剪或降采样，也不对单个场景进行专门挑选。预处理方面，统一采用 global max normalization。评价指标沿用第~\ref{sec:exp-setup} 节：全观测重构报告 RMSE、NMSE(dB) 与 SAM；随机缺失补全则以仅在缺失项上计算的 RMSE*、NMSE(dB)* 与 SAM* 作为主指标。对于补全实验，Tucker 与 NTD-PL 在同一场景、同一缺失率和同一随机种子下共享观测掩膜，以保证配对比较的公平性。

参数方面，CAVE 全观测重构在正文中报告三组秩预算 $(24,24,4)$、$(33,33,4)$ 和 $(40,40,5)$，用于观察不同压缩水平下的稳定趋势；CAVE 随机缺失补全则统一考察 $\rho=0.1,0.3,0.5,0.7$ 四个缺失率，并在相同秩与相同训练协议下比较 Tucker 与 NTD-PL。后续的机制区分、hard-pixel / boundary-region 分析以及阶数机制分析，都建立在这一统一补全协议之上，以避免由于任务设置变化而引入额外干扰。

跨数据集鲁棒性验证不再沿用单一固定秩，而是以统一压缩率匹配规则为每个数据集自动确定一组秩。对于这些补充数据集，补全任务统一固定随机逐元素缺失率 $\rho=0.5$。因此，正文中的跨数据集比较并不是针对某个数据集单独调参后的最优结果，而是在统一协议下对不同真实场景进行的外部有效性检验。

\subsubsection{全观测重构}

我们首先考察全观测设置下的低秩重构性能。表~\ref{tab:cave_recon} 汇报了三组参数预算下的场景均值结果。可以看到，在 $(24,24,4)$、$(33,33,4)$ 和 $(40,40,5)$ 三组设置中，NTD-PL 均 比 Tucker 取得了更低的 RMSE、更低的 NMSE(dB) 以及更低的 SAM。该结果说明，在保持 Tucker 低秩骨架参数规模不变的前提下，由多项式链接带来的稳定收益。

\begin{table}[htbp]
    \centering
    \caption{CAVE 全观测低秩重构结果。三组 rank 下，NTD-PL 和 Tucker 同参数量的条件下，均取得更低的 RMSE、NMSE(dB) 和 SAM。}
    \label{tab:cave_recon}
    \small
    \setlength{\tabcolsep}{5.0pt}
    \input{inputs/cave-representation/recon_summary_pub.tex}
\end{table}

从数值上看，这种收益在不同预算下都较为一致。在较低预算 $(24,24,4)$ 下，NTD-PL 已经将 RMSE 从 $0.0299$ 降至 $0.0256$，同时将 SAM 从 $17.42^\circ$ 降至 $15.15^\circ$；在主报告设置 $(33,33,4)$ 下，RMSE 进一步从 $0.0261$ 降至 $0.0223$，SAM 从 $16.43^\circ$ 降至 $14.41^\circ$；在更高预算 $(40,40,5)$ 下，这种优势仍然保持。也就是说，即使线性 Tucker 的秩已经提高，真实高光谱张量中仍然存在无法被纯多线性机制充分吸收的系统残差，而 NTD-PL 能够在共享低秩骨架上进一步对其进行修正。

进一步地，我们考察这种优势是否仅由少数场景驱动。表~\ref{tab:cave_recon_significance} 与图~\ref{fig:cave_scene_improvement} 给出的场景级对比表明，在 $(33,33,4)$ 下，NTD-PL 在 $14/15$ 个场景上改善了 RMSE，在 $14/15$ 个场景上改善了 NMSE(dB)，并在 $13/15$ 个场景上改善了 SAM。图~\ref{fig:cave_scene_improvement} 中，绝大多数场景上的配对连线都向 NTD-PL 更优的一侧偏移，说明该优势并非由少数异常样本抬高均值，而是在场景级上具有明显的一致性。这表明 NTD-PL 的收益更接近于对高光谱数据中普遍存在的非线性失配进行系统补偿。

\begin{table}[htbp]
    \centering
    \caption{CAVE 全观测重构的场景级对比。NTD-PL 在绝大多数场景上优于 Tucker，说明整体增益并非由少数异常场景驱动。}
    \label{tab:cave_recon_significance}
    \small
    \setlength{\tabcolsep}{5.2pt}
    \input{inputs/cave-representation/recon_significance_summary.tex}
\end{table}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\linewidth]{./inputs/figures/main/cave_reconstruction_scene_gain.pdf}
    \caption{CAVE 全观测重构的场景级对比可视化。每条连线连接同一场景下 Tucker 与 NTD-PL 的结果；整体上，大多数场景都朝 NTD-PL 更优的一侧移动，说明其优势在场景层面具有一致性。}
    \label{fig:cave_scene_improvement}
\end{figure}

空间可视化结果进一步支持这一判断。图~\ref{fig:cave_full_visual} 给出了三个代表性场景的 pseudo-RGB 重构、逐像素误差图以及误差下降图。可以看到，NTD-PL 的收益并不是均匀涂抹式的，而是更集中地出现在结构边界、局部高频纹理和材料过渡区域。例如，在 \texttt{beads} 场景中，珠粒边缘与遮挡交界处的误差下降最为明显；在 \texttt{cd} 场景中，圆盘边缘、中心孔轮廓以及盘面干涉高光与暗部的过渡带误差下降尤为明显；在 \texttt{feathers} 场景中，羽毛细丝的高频纹理、羽片层叠交叠处及其与背景的边界区域误差下降最为突出。这说明 NTD-PL 的额外表达能力并非简单地改变整体亮度响应，而是在空间上更有针对性地修正线性低秩模型难以拟合的局部结构。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{./inputs/figures/main/cave_reconstruction_visual_grid.pdf}
    \caption{CAVE 全观测重构的空间可视化。各行对应一个代表场景；展示原图、重构图、逐像素误差图以及误差下降图。最后一列中正值表示 NTD-PL 相对 Tucker 进一步降低了重构误差。}
    \label{fig:cave_full_visual}
\end{figure}

图~\ref{fig:cave_full_spectral} 从谱线角度给出了相同结论。对所选代表性像素，Tucker 往往能够恢复总体趋势，但在峰值位置、谷值深度或局部转折处仍存在明显偏差；相比之下，NTD-PL 在这些位置通常更贴近真值。可以看到 NTD-PL 对局部谱形的修正不仅降低了 RMSE，也显著改善了谱角误差。这说明前述场景均值和空间误差图中的收益，确实对应于更合理的逐波段恢复，而不是仅由少数强度偏移造成的表面改进。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{./inputs/figures/main/cave_reconstruction_spectra.pdf}
    \caption{CAVE 全观测重构的代表性谱线对比。给出对应像素的真值谱线与重构谱线。标注中的 $\Delta$RMSE 和 $\Delta$SAM 表示相对 Tucker 的改进量。}
    \label{fig:cave_full_spectral}
\end{figure}

综合来看，CAVE 上的全观测实验给出了一条较完整的证据链：第一，NTD-PL 在匹配的参数预算下稳定优于 Tucker；第二，这种优势在场景级层面具有广泛一致性；第三，收益在空间上主要体现为局部误差下降，在光谱上则体现为对关键谱形的更准确恢复。由此可以认为，真实高光谱数据中的确存在可由共享低秩骨架上的显式低阶非线性进一步吸收的残差结构，而这正是 NTD-PL 相对线性 Tucker 的主要收益来源。

\subsubsection{随机观测补全}

在随机缺失观测设置下，我们进一步考察 NTD-PL 在缺失位置恢复上的表现。表~\ref{tab:cave_random_completion} 给出了四个缺失率下的主结果。可以看到，在 $\rho=0.1,0.3,0.5,0.7$ 的全部设置中，NTD-PL 都持续优于 Tucker，RMSE* 和 SAM* 都得到明显改善。这说明 NTD-PL 的优势并不局限于低缺失率下的轻微修正，而是在从轻度缺失到高度缺失的整个区间内都保持稳定。尤其在最高缺失率 $\rho=0.7$ 下，RMSE* 的改善幅度进一步增大，表明当可用观测更稀疏时，非线性对缺失位置恢复仍然具有明显作用。

\begin{table}[htbp]
    \centering
    \caption{CAVE 随机缺失补全的主结果。四个缺失率下NTD-PL 均优于 Tucker，说明收益直接体现在缺失位置的恢复质量上。}
    \label{tab:cave_random_completion}
    \small
    \setlength{\tabcolsep}{5.0pt}
    \input{inputs/cave-random-completion/random_completion_main_table.tex}
\end{table}

进一步地，我们希望确认这种优势是否同时在逐场景和逐缺失率两个层面成立。表~\ref{tab:cave_completion_significance} 显示，RMSE* 在四个缺失率下均有 $14/15$ 个场景优于 Tucker；SAM* 的胜场数仍分别达到 $14/15$、$13/15$、$13/15$ 和 $12/15$。与此同时，Wilcoxon 检验与 sign test 的 $p$ 值在各缺失率下都保持显著。这说明补全中的优势并不是由少数场景或单一缺失率偶然造成的，而是在逐场景和逐缺失率上都具有稳定的一致性。

\begin{table}[htbp]
    \centering
    \caption{CAVE 随机缺失补全的场景级对比。各缺失率下统一采用 Tucker $-$ NTD-PL 的差值定义，因此正值表示 NTD-PL 更优。结果表明，补全收益在不同缺失率下都具有显著的场景级一致性。}
    \label{tab:cave_completion_significance}
    \small
    \setlength{\tabcolsep}{5.0pt}
    \input{inputs/cave-random-completion/random_completion_significance_summary.tex}
\end{table}

为了进一步说明这种收益确实发生在缺失位置的恢复上，而不是整体平滑带来的表面改善，图~\ref{fig:cave_completion_visual} 给出了两个代表性场景的可视化结果。综合来看，随机缺失补全实验进一步强化了前一小节的结论：第一，NTD-PL 的优势不仅体现在全观测重构中，也体现在真正的缺失恢复任务上；第二，这种优势在多个缺失率下都保持稳定，并且在 scene-wise 与 rate-wise 两个层面同时成立；第三，空间可视化表明收益确实发生在 missing 区域本身，而不是由 all-entry 指标中的已观测位置掩盖或放大。换言之，CAVE 上的补全实验说明，显式条目层多项式链接不仅能够改进低秩近似，还能够在观测不完整时更有效地恢复真实高光谱结构。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{./inputs/figures/main/cave_completion_visual_grid.pdf}
    \caption{CAVE 随机缺失补全的代表性空间可视化。各列依次为原图、带掩膜的观测输入、Tucker 补全、NTD-PL 补全、Tucker 的 missing-only 误差、NTD-PL 的 missing-only 误差，以及 missing-only 误差下降图。最后一列按 Tucker $-$ NTD-PL 定义，正值表示 NTD-PL 在缺失位置上进一步降低了补全误差。}
    \label{fig:cave_completion_visual}
\end{figure}

\subsubsection{多项式后处理的机制区分}

前两小节已经表明，NTD-PL 在全观测重构与随机缺失补全中都稳定优于 Tucker。但一个仍需进一步回答的问题是：这种收益究竟来自什么？一种解释则是，NTD-PL 的优势也许并不需要联合建模，只要先得到 Tucker 重构，再对其输出施加一个逐元素多项式校准即可。为区分这些可能性，我们引入一个后处理基线 \emph{PolyCal}。该方法先训练标准 Tucker，再在其输出上拟合一个共享的逐元素四阶多项式响应映射，从而在不改变低秩骨架的前提下对条目层响应进行后处理修正。

表~\ref{tab:mechanism_closure} 汇报了这一机制对照的主结果。可以看到，\emph{PolyCal} 后处理相对 Tucker 只能带来有限的改善，而 NTD-PL 则展现出更显著的性能提升，说明简单的后处理并不能替代 NTD-PL 的多项式与低秩骨架的联合建模。

\begin{table}[htbp]
    \centering
    \caption{机制对照主表。三种方法在全观测重构与 $\rho=0.5$ 的随机缺失补全上进行比较。PolyCal 在 Tucker 输出后施加共享逐元素多项式校准，而 NTD-PL 则将非线性链接与低秩骨架联合建模。}
    \label{tab:mechanism_closure}
    \small
    \setlength{\tabcolsep}{4.8pt}
    \input{inputs/cave-random-completion/mechanism_closure_main_table.tex}
\end{table}

图~\ref{fig:mechanism_closure_main} 以四个 panel 更直观地展示了这一点：无论是在全观测重构的 RMSE、SAM，还是在补全任务的 RMSE*、SAM* 上，三种方法都呈现出一致的关系。Tucker 到 PolyCal 的下降说明“后处理式响应修正”确实有意义，而 PolyCal 到 NTD-PL 的继续下降则说明，真正关键的并不只是对输出做一个逐元素多项式拟合，而是让这种非线性链接在训练过程中与低秩骨架共同决定表示结构。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{./inputs/figures/main/mechanism_closure_main_figure.pdf}
    \caption{机制区分主图。四个 panel 分别比较全观测重构的 RMSE、SAM 以及 $\rho=0.5$ 补全的 RMSE*、SAM*。三种方法都呈现出一致改进关系，说明后处理只能部分逼近 NTD-PL 的收益，而无法替代其联合建模。}
    \label{fig:mechanism_closure_main}
\end{figure}

因此，这一小节给出的结论是明确的：第一，NTD-PL 的收益不能解释为增参；第二，PolyCal 虽然能够吸收一部分条目层响应偏差，但只能部分逼近 NTD-PL 的表现；第三，真正关键的是在训练过程中将显式非线性链接与共享低秩骨架联合建模，而不是在得到线性低秩结果之后再附加一个统一的后处理映射。这也从机制层面解释了为什么 NTD-PL 不应被理解为对 Tucker 输出的简单校准，而应被看作一种对 Tucker 条目生成机制的结构化扩展。

\subsubsection{优势出现的位置}

本节考虑的问题是额外收益究竟出现的位置。如果 NTD-PL 只是对整幅图像做更平滑的全局修正，那么其增益应当近似均匀分布；相反，如果它主要在修正线性低秩模型最难拟合的局部结构，那么收益应当更集中地出现在高难度位置以及结构边界附近。为回答这一问题，我们从困难度与边界两个维度对恢复误差的改进进行分组统计。

具体地，我们将每个空间位置处的完整光谱视为一个向量，并以 Tucker 在该位置上 RMSE 作为困难分数，即分数越大，表示该像素对线性低秩补全越困难。与此同时，以空间梯度幅值在光谱维上的平均作为边界分数，用于刻画该位置是否处于边界、纹理或局部过渡区域。随后，在每个场景内部，分别按困难分数和边界分数将像素划分为四个 quartiles，再对不同场景上的统计量进行汇总。图~\ref{fig:cave_completion_advantage_heatmap} 清楚地表明，NTD-PL 的收益并不是均匀分布的。沿困难度方向看，增益随难度升高而显著增强：从最简单 quartile 到最困难 quartile，平均 $\Delta$RMSE* 由接近零甚至略为负值，逐步上升到明显正值；对应的 $\Delta$SAM* 也表现出相同趋势。说明 NTD-PL 的主要作用，并不是在 Tucker 已经足够好的容易区域上继续细修，而是在 Tucker 最难拟合的位置上提供更有效的补偿。沿边界度方向看，趋势同样存在。随着边界分数增大，NTD-PL 相对 Tucker 的平均增益整体上升，说明空间边界、局部纹理和材料过渡区域更容易从显式条目层非线性中获益。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{./inputs/figures/main/cave_completion_advantage_heatmap.pdf}
    \caption{NTD-PL 相对 Tucker 的局部收益在困难 quartile 与 边界 quartile 上的二维分布，正值表示 NTD-PL 更优。可以看到，收益并非均匀分布，而是主要集中在困难与边界的交叉区域。}
    \label{fig:cave_completion_advantage_heatmap}
\end{figure}

为了把图~\ref{fig:cave_completion_advantage_heatmap} 中的分组统计进一步落实到具体空间位置，图~\ref{fig:cave_completion_advantage_spatial} 给出了一个按相同协议选出的代表性场景。四个 panel 从左到右分别展示 pseudo-RGB 原图、Tucker 的 difficulty map、boundary score map，以及 NTD-PL 相对 Tucker 的 missing-only error reduction map。这里，difficulty 由 Tucker 在缺失位置上的 RMSE* 定义，用来刻画线性低秩补全在该位置的恢复难度；boundary score 则由空间梯度幅值在光谱维上的平均得到，用来表征该位置是否处于边界、纹理或局部过渡区域。图中的黄轮廓、青轮廓和白轮廓分别表示 top difficulty 区域、top boundary 区域以及两者的交叉区域。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{./inputs/figures/main/cave_completion_advantage_spatial_case.pdf}
    \caption{代表性场景上的局部收益定位。四列依次为 pseudo-RGB 原图、Tucker 的 difficulty map（以缺失位置上的 RMSE* 定义）、boundary score map，以及 NTD-PL 相对 Tucker 的 missing-only error reduction map。黄轮廓表示 top difficulty 区域，青轮廓表示 top boundary 区域，白轮廓表示两者的交叉区域。图中可以看到，正增益主要集中在高难度与强边界同时出现的位置，说明 NTD-PL 的补全优势具有明确的空间定位。}
    \label{fig:cave_completion_advantage_spatial}
\end{figure}

从空间分布上看，NTD-PL 的正增益并不是在整幅图像上近似均匀地铺开，而是明显集中在白色轮廓附近。换句话说，最显著的误差下降主要出现在两类条件同时满足的位置：一类是 Tucker 本身恢复较困难的位置，另一类是局部空间变化较剧烈的位置。这样的区域通常对应材料交界、细粒纹理、边缘过渡或局部混合更复杂的部位，因此也是线性低秩假设更容易产生系统残差的地方。相较之下，NTD-PL 在保持共享低秩骨架不变的前提下，通过显式条目层多项式链接对这些局部残差进行了进一步修正。因此，这张图给出一个空间层面的机制解释：NTD-PL 的收益具有明确的空间定位，主要集中在 hardest pixels 与 boundary regions 的交叠区域，而不是来自整幅图像上的均匀平滑式改进。

\subsubsection{多项式次数考察}

为考察 NTD-PL 的收益主要由哪些阶次提供，我们在与前文相同的 CAVE 随机缺失补全设置下扫描多项式最高阶数 $P$，并记录 missing-only 指标及各阶项的有效贡献。图~\ref{fig:parameter_mechanism} 左图表明，随着 $P$ 从 $1$ 提升到 $4$，补全性能持续改善；但主要收益集中在低到中阶范围内，继续提高阶数后增益已明显减弱。右图进一步显示，模型贡献主要由二阶和三阶项承担：当 $P\ge 3$ 时，这两项始终占主导，而四阶项仅提供较小的边际修正。这说明，真实高光谱补全中的主要失配并不需要很高阶的自由度来刻画。对 NTD-PL 而言，低到中阶的显式非线性已经足以在共享 Tucker 骨架上吸收大部分条目层残差，这也说明其额外复杂度是受控的。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\linewidth]{./inputs/figures/main/cave_parameter_mechanism.pdf}
    \caption{CAVE 随机缺失补全中的阶数机制分析。左图给出缺失位置主指标随多项式最高阶数 $P$ 的变化，显示主要收益在低到中阶范围内逐步建立；右图给出不同 $P$ 下各阶项的平均 effective contribution，表明二阶和三阶项始终占主导，而更高阶项的边际贡献明显较弱。}
    \label{fig:parameter_mechanism}
\end{figure}

\subsubsection{跨数据集的鲁棒性验证}

为了检验上述结论是否仅在 CAVE 上成立，我们进一步在 Jasper Ridge、Samson、Urban 和 Cuprite 上进行外部验证，并同时考察全观测重构与随机缺失补全。所有数据集均采用全场景设置、global max normalization，并按统一压缩率匹配规则自动确定秩；补全任务统一固定随机逐元素缺失率 $\rho=0.5$。表~\ref{tab:real_hsi_robustness} 表明，NTD-PL 的优势并非 CAVE-specific。在 Jasper Ridge、Samson 和 Urban 上，无论重建还是补全，NTD-PL 均优于 Tucker，且 $\Delta$NMSE(dB) 与 $\Delta$SAM 大多保持同方向改善。整体上，RMSE 口径下 $8$ 个 dataset $\times$ task 组合中有 $7$ 个为正收益，说明其优势在不同场景与任务上具有较好的稳定性。Cuprite 则提供了一个清晰的边界案例：在该场景下，NTD-PL 的收益明显减弱，部分指标上 Tucker 略优。这说明当数据更接近经典线性混合、条目层非线性偏离较弱时，Tucker 已能解释主要结构，此时多项式链接只能带来有限的边际改进。因此，NTD-PL 的结论应理解为：它在多数真实高光谱场景中都能稳定改进 Tucker，但这种优势并非无条件成立，其有效性仍取决于数据中条目层非线性残差的强弱。

\begin{table}[htbp]
    \centering
    \caption{跨数据集鲁棒性验证主表。Rank 为按统一压缩率匹配规则自动确定的 multilinear rank。Recon. 行中 Tucker / NTD-PL 为 RMSE，Compl. 行中 Tucker / NTD-PL 为 RMSE*；Gain(\%)、$\Delta$NMSE(dB) 与 $\Delta$SAM 统一按 Tucker $-$ NTD-PL 定义，因此正值表示 NTD-PL 更优。}
    \label{tab:real_hsi_robustness}
    \small
    \setlength{\tabcolsep}{4.6pt}
    \input{inputs/real-hsi-robustness/real_hsi_robustness_main_table.tex}
\end{table}
