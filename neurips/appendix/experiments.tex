\section{Additional Experiments}

\subsection{Controlled Synthetic Mechanism Checks}
\label{app:controlled-synthetic}

The controlled experiments in the main text are designed to isolate the role
of the scalar link. Table~\ref{tab:linear-consistency-controlled} first checks
a negative control: when the source is linear Tucker, NTD-PL does not improve
by inventing nonlinear terms. The only exception is an affine offset, where
the constant link term is the intended correction.

For the nonlinear controlled tensors, the nonlinearity is introduced as an
orthogonal residual rather than by directly setting \(Y=g(S^\star)\). Let
\(S^\star\) be the linear low-rank Tucker backbone and let \(g\) be one of the
candidate scalar responses. We form
\begin{equation}
  R^\star
  =
  g(S^\star)
  -
  \frac{\langle g(S^\star),S^\star\rangle}{\|S^\star\|_F^2}\,S^\star,
  \label{eq:controlled-orthogonal-residual}
\end{equation}
and, when \(\|R^\star\|_F>0\), normalize it as
\begin{equation}
  \widetilde R^\star
  =
  \frac{\|S^\star\|_F}{\|R^\star\|_F}R^\star .
  \label{eq:controlled-normalized-residual}
\end{equation}
The observed tensor is then
\begin{equation}
  Y_\alpha
  =
  \sqrt{1-\alpha}\,S^\star
  +
  \sqrt{\alpha}\,\widetilde R^\star,
  \qquad 0\le \alpha\le 1 .
  \label{eq:controlled-residual-mixture}
\end{equation}
By construction, \(\langle S^\star,\widetilde R^\star\rangle=0\) and
\(\|\widetilde R^\star\|_F=\|S^\star\|_F\), so \(\alpha\) is exactly the
energy fraction assigned to the nonlinear residual. This keeps the total
energy fixed while moving the observation away from the linear Tucker
backbone. The candidate residual families are \(g(s)=s^2+s^3\),
\(g(s)=(\exp(\kappa s)-1)/\kappa\), and \(g(s)=\tanh(\kappa s)\),
with \(\kappa\) set from the input range.

The main text reports the nonlinear alpha sweep and degree sweep.
Figure~\ref{fig:controlled-training-curves} shows that degree continuation
activates higher powers without destabilizing the fit.
Table~\ref{tab:controlled-beta-contribution} complements those figures with a
small but consistent term-mass concentration effect: the dominant contribution
usually lands on the degree that matches the injected response, while smooth
odd links spread mass across nearby odd orders.

\begin{table}[ht]
  \centering
  \caption{Linear consistency on synthetic Tucker tensors. The \(p>1\)
  columns report effective higher-order NTD-PL contribution.}
  \label{tab:linear-consistency-controlled}
  {\small
  \setlength{\tabcolsep}{4.5pt}
  \renewcommand{\arraystretch}{1.06}
  \input{tables/controlled_linear_consistency.tex}
  }
\end{table}

\begin{figure}[ht]
  \centering
  \includegraphics[width=0.98\linewidth]{figures/controlled_nonlinear_step_strip.pdf}
  \caption{NTD-PL training curves for the controlled nonlinear mechanism
  checks at \(\alpha=0.25\) and \(P=5\). Dashed vertical lines mark
  degree-activation events.}
  \label{fig:controlled-training-curves}
\end{figure}

\begin{table}[ht]
  \centering
  \caption{Representative effective NTD-PL term contributions under moderate
  nonlinearity, reported as percentages over ten seeds. Polynomial targets
  concentrate mass at the matching nonlinear degrees, while odd smooth links
  place most nonlinear mass on odd powers.}
  \label{tab:controlled-beta-contribution}
  {\small
  \setlength{\tabcolsep}{4pt}
  \renewcommand{\arraystretch}{1.08}
  \input{tables/controlled_beta_contribution.tex}
  }
\end{table}

\FloatBarrier

\subsection{CAVE Completion Significance and Robustness}
\label{app:single-rank-completion}

The main text reports the full-resolution random-missing CAVE completion table
at rank \((33,33,4)\). We do not repeat that table here; instead,
Table~\ref{tab:cave-completion-significance} gives the paired sign and
Wilcoxon tests for the same protocol.

\begin{table}[ht]
  \centering
  \caption{Significance tests for full-resolution CAVE completion at rank
  \((33,33,4)\). Metrics are computed on missing entries.}
  \label{tab:cave-completion-significance}
  \scriptsize
  \resizebox{\linewidth}{!}{\input{tables/cave_completion_significance.tex}}
\end{table}

\begin{table}[ht]
  \centering
  \caption{Link Refresh diagnostic on CAVE completion. Low/mid/high gains
  report the mean NTD-PL RMSE$^\ast$ gain over Tucker within each
  \(D_{\mathrm{link}}\) tercile.}
  \label{tab:ntdpl-polycal-diagnostic}
  \scriptsize
  \resizebox{0.94\linewidth}{!}{\input{tables/ntdpl_polycal_diagnostic.tex}}
\end{table}

\subsection{Link Refresh Residual Diagnostic}
\label{app:posthoc-calibration-baselines}

The diagnostic used in the main text reuses the Link Refresh step from
Section~\ref{sec:optimization} as a one-round residual probe. Let
\(\widehat{\cY}_{\mathrm{T}}\) denote the Tucker output and \(\cY\) the target
tensor. Starting from this fitted Tucker latent tensor, we update only the
scalar polynomial coefficients by fitting
\[
  h_{\gamma}(x)=\sum_{p=0}^{4}\gamma_p x^p
\]
by ridge regression on pairs
\((\widehat Y_{\mathrm{T},\mathbf i},Y_{\mathbf i})\). In full reconstruction,
all entries are used for this fit. In completion, the fit uses only observed
entries and is then evaluated on held-out entries. The fitted map is applied
entrywise to \(\widehat{\cY}_{\mathrm{T}}\). We summarize this cheap refresh by
the Link Refresh diagnostic
\[
  D_{\mathrm{link}}
  =10\log_{10}\!\left(
  \frac{\|\cY_{\mathrm{eval}}-\widehat{\cY}_{\mathrm{T}}\|_F^2}
  {\|\cY_{\mathrm{eval}}-\widehat{\cY}_{\mathrm{link}}\|_F^2}
  \right),
\]
which measures, on a log scale, how much Tucker residual energy is removed on
the task-specific evaluation entries. In other words, it measures how much
shared scalar-link yield is already present before the full NTD-PL alternating
solver starts reshaping the Tucker parameters.

\subsection{Joint Link-Family Smoke Test}
\label{app:joint-link-family}

The main model uses a polynomial link because it gives a closed-form ridge
coefficient update and a direct Veronese rank-lift interpretation. To check
whether the improvement is tied to the polynomial basis itself, we ran a
small joint link-family smoke test on three CAVE scenes at spatial resolution
\(256\times256\) and backbone rank \((18,18,3)\). The spline variant replaces
the polynomial link by a one-dimensional piecewise-linear link with eight
knots. Its knot values are updated by ridge regression on the current latent
entries, while the Tucker backbone is updated jointly through the link
derivative.

\begin{table}[ht]
  \centering
  \caption{Joint scalar-link family smoke test on CAVE reconstruction. The
  spline link is slightly more flexible but slower; NTD-PL is the analyzable
  polynomial member used in the main paper.}
  \label{tab:cave-joint-link-ablation}
  \scriptsize
  \resizebox{0.92\linewidth}{!}{\input{tables/cave_joint_link_ablation.tex}}
\end{table}

\subsection{CAVE Beads Pseudo-RGB Visualization}
\label{app:cave-beads-visualization}

Figure~\ref{fig:cave-beads-recon-completion} visualizes the CAVE beads scene
under the reconstruction and random-completion protocols. Each 31-band tensor
is rendered as pseudo-RGB by selecting three representative spectral bands
near \(75\%\), \(50\%\), and \(20\%\) of the wavelength index range and applying
a shared linear intensity scaling within each task. For completion, unobserved
entries in the observation panel are shown in gray.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.98\linewidth]{figures/cave_beads_recon_completion_visual.pdf}
  \caption{CAVE beads visual comparison. Rows correspond to reconstruction
  and random completion at missing rate \(0.5\). Error maps show Tucker minus
  NTD-PL per-pixel RMSE, so positive values indicate lower NTD-PL error.}
  \label{fig:cave-beads-recon-completion}
\end{figure}

\subsection{Cross-Domain Real Tensor Checks}
\label{app:cross-domain-real-tensors}

Table~\ref{tab:real-tensor-reconstruction} reports matched-rank
reconstruction on tensors outside the main hyperspectral setting: additional
hyperspectral scenes, natural-image collections, and object-view tensors. The
results are consistent with the scoped claim in
Section~\ref{subsec:external-hsi-validity}: gains are clearest on
hyperspectral and object-view data, while natural-image collections provide
more modest improvements.

\begin{table}[ht]
  \centering
  \caption{Additional matched-rank real tensor reconstruction. Columns report
  RMSE; gains are relative RMSE reductions over Tucker, and positive
  \(\Delta\)RMSE favors NTD-PL.}
  \label{tab:real-tensor-reconstruction}
  \scriptsize
  \resizebox{\linewidth}{!}{\input{tables/real_tensor_reconstruction_main.tex}}
\end{table}

\subsection{Non-HSI Link Refresh Diagnostics}
\label{app:nonhsi-link-diagnostics}

The main diagnostic table uses per-unit non-HSI experiments rather than the
collection-level tensors in Table~\ref{tab:real-tensor-reconstruction}. CBSD68
uses per-image units resized to \(96\times96\times3\) with rank
\((32,32,3)\); the available diagnostic CSV contains 16 CBSD68 units. CIFAR-10
uses 1000 class-balanced test images of shape \(32\times32\times3\) with rank
\((20,20,3)\). COIL-100 uses one object at a time as a
\(36\times48\times48\times3\) view tensor with rank \((8,12,12,2)\). smallNORB
uses one object instance at a time as an \(18\times64\times64\times2\) stereo
view tensor with rank \((12,24,24,2)\). All units are max-normalized
independently, and diagnostic labels are assigned only from
\(D_{\mathrm{link}}\), not from NTD-PL gain.

\begin{table}[ht]
  \centering
  \caption{Representative non-HSI units selected by \(D_{\mathrm{link}}\).
  Boundary/effective labels are assigned before inspecting NTD-PL gain.}
  \label{tab:nonhsi-link-representatives}
  \scriptsize
  \setlength{\tabcolsep}{4pt}
  \resizebox{\linewidth}{!}{
  \begin{tabular}{@{}l l l l c c c c@{}}
    \toprule
    Domain & Dataset & Unit & Label & \(D_{\mathrm{link}}\) &
    Tucker RMSE & NTD-PL RMSE & Gain \\
    \midrule
    Natural images & CIFAR-10 & cifar10\_test\_06970 & boundary &
    -0.136 & 0.0008 & 0.0008 & -4.46\% \\
    Natural images & CBSD68 & cbsd68\_011 & moderate &
    0.077 & 0.0409 & 0.0392 & 4.25\% \\
    Natural images & CBSD68 & cbsd68\_012 & effective &
    0.341 & 0.0490 & 0.0408 & 16.69\% \\
    Object-view & COIL-100 & coil\_obj088 & boundary &
    0.017 & 0.0280 & 0.0276 & 1.51\% \\
    Object-view & COIL-100 & coil\_obj091 & effective &
    0.875 & 0.0751 & 0.0650 & 13.34\% \\
    Object-view & smallNORB & smallnorb\_c3\_i4 & boundary &
    0.077 & 0.0315 & 0.0301 & 4.51\% \\
    Object-view & smallNORB & smallnorb\_c2\_i7 & effective &
    1.048 & 0.0478 & 0.0380 & 20.53\% \\
    \bottomrule
  \end{tabular}}
\end{table}

\begin{figure}[ht]
  \centering
  \includegraphics[width=\linewidth]{figures/nonhsi_dlink_scatter_3domain.pdf}
  \caption{\(D_{\mathrm{link}}\) versus NTD-PL RMSE gain over non-HSI tensor
  units, shown as per-dataset panels for natural-image, object-view, and
  action-video settings. Units are selected independently and labels are assigned from
  \(D_{\mathrm{link}}\).}
  \label{fig:nonhsi-link-scatter}
\end{figure}
